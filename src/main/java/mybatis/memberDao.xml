<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.coksabu.yojic.lesson.member.dao.MemberDao">
		
	<select id="checkEmail" resultType="Integer" >
		select count(*) from members where email=#{email}
	</select>
	
	<select id="checkPhone" resultType="Integer" >
		select count(*) from members where phone=#{phone}
	</select>
	
	<select id="checkPassword" parameterType="com.coksabu.yojic.lesson.member.model.Login" resultType="String" >
		select password from members where email= #{email}
	</select>
	
	<select id="takeImgUniv" resultType="com.coksabu.yojic.lesson.member.model.Profile">
		select imgPath, universe, univsub, academy, academyd from profile where email=#{email} 
	</select>
	
	<select id="confirmCertify" resultType="Integer">
		select count(*) from lessoncertify where email= #{email}
	</select>
	
	<update id="membersipAndCetifyToZero">
	 	update members as A, profile as B set A.membership=0, B.certify=0, B.certifydate=now() where A.email=#{email} and A.email=B.email
	</update>
	
	<insert id="signUp" parameterType="com.coksabu.yojic.lesson.member.model.MemberInfo" >
		insert into members (email, password, name, nickname, birth, phone, logindate, signupdate, membership, enabled, authority, human_status, fail_count, fail_time, imgpath, push) values (#{email}, #{password}, #{name}, #{name}, #{birth}, #{phone}, now(), now(), 0, 1, 'user', 0, 0, 0, 'pro.png', 'ON')
	</insert>
	
	<insert id="naverSignUp" parameterType="com.coksabu.yojic.lesson.member.model.MemberInfo" >
		insert into members (email, password, name, nickname, birth, phone, logindate, signupdate, membership, enabled, authority, human_status, fail_count, fail_time, imgpath, push) values (#{email}, #{password}, #{name}, #{name}, #{birth}, #{phone}, now(), now(), 0, 1, 'user', 0, 0, 0, 'pro.png', 'ON')
	</insert>
	
	<update id="updateNaverPassword" parameterType="com.coksabu.yojic.lesson.member.model.MemberInfo">
	 	update members set password=#{password} where email=#{email}
	</update>
	
	<select id="takeEncryptedPW" resultType="String">
	 	select password from members where email=#{email}
	</select>
	
	<select id="checkImgPath" resultType="Integer">
		select count(*) from profile where email = #{email}
	</select>

	<select id="takeImgPath" resultType="String">
		select imgpath from profile where email = #{email}
	</select>
	
	<insert id="writeProfile" parameterType="com.coksabu.yojic.lesson.member.model.TeacherDB" >
		insert into profile (email, imgpath, locales, universe, univsub, universer, career, sexual, academy, academyd, nation, nationy, toeic, tofle, teps, hsk, jpt, singular, certify) values 
		(#{email}, #{imgPath}, #{locales}, #{universe}, #{univsub}, #{universer}, #{career}, #{sexual}, #{academy}, #{academyd}, #{nation}, #{nationy}, #{toeic}, #{tofle}, #{teps}, #{hsk}, #{jpt}, #{singular}, 0)
	</insert>
	
	<update id="updateMembersImgpath"  parameterType="String">
		update members set imgpath=#{imgpath} where email=#{email}
	</update>
	
	
	<update id="updateProfile"  parameterType="com.coksabu.yojic.lesson.member.model.TeacherDB">
		update profile as A, members as B set A.imgpath=#{imgPath}, A.locales=#{locales}, A.sexual=#{sexual}, A.universe=#{universe}, 
		A.univsub=#{univsub}, A.universer=#{universer}, A.career=#{career} , A.academy=#{academy}, A.academyd = #{academyd},
		A.nation=#{nation}, A.nationy=#{nationy}, A.toeic=#{toeic}, A.tofle=#{tofle}, A.teps=#{teps}, A.hsk=#{hsk}, A.jpt=#{jpt}, 
		 A.singular=#{singular}, B.imgpath=#{imgPath} where A.email=#{email} and B.email=#{email}
	</update>
	
	<select id="takeProfile" resultType="com.coksabu.yojic.lesson.member.model.Profile" >
		select A.nickname, A.birth ,B.imgpath, B.locales, B.universe, B.univsub, B.academy, B.academyd, B.career, B.sexual, B.certify from members as A, profile as B where A.email =#{email} and B.email=#{email}
	</select>
	
	<select id="takeProfileList" resultType="com.coksabu.yojic.lesson.member.model.Profile" >
		<foreach collection="list" item="item" separator="union all">
			(select nickname, imgpath from members where email=#{item})
		</foreach>
	</select>

	<select id="takeTeacherDB" resultType="com.coksabu.yojic.lesson.member.model.TeacherDB" >
		select imgpath, locales, universe, universer, career, univsub, sexual, academy, academyd,
		nation, nationy, toeic, tofle, teps, hsk, jpt, singular, certify from profile where email=#{email}
	</select>
	
	<select id="univSearch" resultType="String" >
		select * from universe where universe like concat('%',#{universe},'%')  limit #{firstRow}, #{endRow}
	</select>
	
	<select id="univCount" resultType="int" >
		select count(*) from universe where universe like concat('%',#{universe},'%')
	</select>
	
	<update id="updateImgCertify" parameterType="com.coksabu.yojic.lesson.member.model.CertifyDB" >
		update profile set certifyimg1=#{certifyimg1}, certifyimg2=#{certifyimg2}, certifyimg3=#{certifyimg3}, certify=3, certifydate=now() where email=#{email}
	</update>
	
	<select id="tekeCertify" resultType="com.coksabu.yojic.lesson.member.model.CertifyDB">
		select email, certify, certifyimg1, certifyimg2, certifyimg3 from profile where email=#{email}
	</select>
	
	<select id="readMemberInfo" resultType="com.coksabu.yojic.lesson.member.model.MemberInfo" >
		select email, name, birth, phone, nickname from members where email=#{email}
	</select>
	
	<update id="updatePhone" parameterType="com.coksabu.yojic.lesson.member.model.MemberInfo" >
		update members set phone=#{phone} where email=#{email}
	</update>
	
	<select id="takePassword" resultType="String" >
		select password from members where email=#{email}
	</select>
	
	<update id="updatePassword" parameterType="com.coksabu.yojic.lesson.member.model.Password" >
		update members set password=#{password2}, human_status=0 where email=#{email}
	</update>
	
	<update id="updatePassword2" parameterType="com.coksabu.yojic.lesson.member.model.Password" >
		update members set password=#{password2}, human_status=1, enabled=1, fail_count=0, fail_time=0  where email=#{email}
	</update>
	
	<insert id="insertQna" parameterType="com.coksabu.yojic.lesson.member.model.Qna" >
		insert into qna (email, title, content, visible, date) values (#{email}, #{title}, #{content}, 1, #{date})
	</insert>
	
	<select id="countLessonCertify" resultType="Integer" >
		select count(*) from lessoncertify where email=#{email}
	</select>
	
	<insert id="insertLessonCertify" parameterType="com.coksabu.yojic.lesson.member.model.LessonCertify" >
		insert into lessoncertify (name, serviceAgree, myAgree, email) values (#{name}, #{serviceAgree}, #{myAgree}, #{email} )
	</insert>
	
	
	<select id="readLessonCertify" resultType="com.coksabu.yojic.lesson.member.model.LessonCertify" >
		select name, serviceagree, myagree from lessoncertify where email=#{email}
	</select>
	
	<select id="readName" resultType="String" >
		select name from members where email=#{email}
	</select>
	
	<select id="countEmail" parameterType="com.coksabu.yojic.lesson.member.model.EmailInfo" resultType="Integer">
		select count(*) from members where name=#{name} and phone=#{phone}
	</select>
	
	<select id="takeEmail" parameterType="com.coksabu.yojic.lesson.member.model.EmailInfo" resultType="String">
		select email from members where name=#{name} and phone=#{phone}
	</select>
	
	<update id="updateLoginTime" >
		update members set logindate=#{date}, fail_count=0, fail_time=0 where email=#{email}
	</update>
	
	<insert id="insertApplyCount">
		insert into applycount (email, count) values (#{email}, 0)
	</insert>
	
	<select id="countMyQna" resultType="int">
		select count(*) from qna where email=#{email}
	</select>
	
	<select id="myQnaList" resultType="com.coksabu.yojic.lesson.member.model.Qna">
		select title, content, id, answer, date from qna where email=#{email} order by id desc limit #{firstRow}, #{endRow}
	</select>
	
	<select id="takeHumanStatus" resultType="int">
		select human_status from members where email=#{email}
	</select>
	
	<select id="takeFailCount" resultType="int">
		select fail_count from members where email=#{email}
	</select>
	
	<select id="takeFailTime" resultType="Date">
		select fail_time from members where email=#{email}
	</select>
	
	<update id="addFailCount">
		update members set fail_count= fail_count+1 where email=#{email}
	</update>
	
	<update id="loginDisabled" parameterType="java.util.HashMap">
		update members set fail_count = fail_count+1, enabled=0, fail_time=date_add(now(), interval 15 minute) where email=#{email}
	</update>
	
	<update id="failReset">
		update members set fail_count =0, enabled=1, fail_time=0 where email=#{email}
	</update>
	
	<update id="addFailTime">
		update members set fail_time = date_add(now(), interval 15 minute) where email=#{email}
	</update>
	
	<update id="deletePushToken">
		update members set token=null, device=null where token=#{token}
	</update>
	
	<update id="registerPushToken">
		update members set token=#{token}, device=#{device} where email=#{email}
	</update>
	
	<select id="takeUnReadCount" resultType="int">
		select count(*) from message where message_receiver=#{email} and message_read=1
	</select>

	<select id="takeNickName" resultType="String">
		select nickname from members where email=#{email}
	</select>
	
	<update id="updateNickName">
		update members set nickname=#{nickname} where email=#{email}
	</update>
	
	<select id="takePurchaseCount" resultType="Integer">
		select count(*) from purchasehistory where selleremail=#{email} or buyeremail=#{email}
	</select>
	
	<select id="takeChatTranCount" resultType="Integer">
		select count(*) from chat_transaction where seller like concat('%',#{email},'%') or buyer like concat('%',#{email},'%')
	</select>
	
	<select id="takeMyAccount" resultType="com.coksabu.yojic.lesson.member.model.MyAccount">
		select name, account, accountnumber from members where email=#{email}
	</select>
	
	<update id="updateMyAccount" parameterType="com.coksabu.yojic.lesson.member.model.MyAccount">
		update members set account=#{account}, accountnumber=#{accountNumber} where email=#{email}
	</update>
	
	<select id="myPromotionCount" resultType="Integer">
		select count(*) from promotion where email=#{email}
	</select>
	
	<select id="myPromotion" resultType="com.coksabu.yojic.lesson.member.model.Promotion">
		select * from promotion where email=#{email}
	</select>
	
	<select id="takeSignUpDate" resultType="Date">
		select signupdate from members where email=#{email}
	</select>
	
	<insert id="registerMyPromotion" parameterType="com.coksabu.yojic.lesson.member.model.Promotion">
		insert into promotion (email, title, content, code, startdate, enddate) values (#{email}, #{title}, #{content}, #{code}, #{startDate}, DATE_ADD(#{startDate}, INTERVAL 2 MONTH) )
	</insert>
	
	<select id="myCommissionPromotionCount" resultType="Integer">
		select count(*) from promotion where email=#{email} and (code='COKSABU' or code='LESSON')
	</select>
	
</mapper>