<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.coksabu.yojic.lesson.cron.dao.CronDao">
	
	<delete id="deleteOldWish">
		delete from wishlist where DATE_ADD(wishdate, INTERVAL '14' DAY) &lt; now()
	</delete>
	
	<select id="takeApplyId" resultType="Integer" >
		select id from apply where DATE_ADD(postingdate, INTERVAL '1' year) &lt; now()
	</select>
	
	<delete id="deleteOldLessonApply">
		delete from apply where DATE_ADD(postingdate, INTERVAL '1' year) &lt; now()
	</delete>
	
	<delete id="deleteOldLessonApplyList" parameterType="java.util.List"> 
		delete from applylist
			<where>
			<foreach collection="list" item="item" open="" close="" separator="or">
			 apply_id=#{item}
			</foreach>
			</where> 
	</delete>
	
	<update id="resetApplyCount">
		update applycount set count=0
	</update>
	
	<update id="resetLoginFailCount">
		update members set fail_count = 0, enabled=1, fail_time=0
	</update>
	
	<update id="resetEndDateService">
		update purchasehistory set orderstatus=#{val} where orderstatus=#{condition} and DATE_ADD(enddate, INTERVAL 1 DAY) &lt; now() 
	</update>
		
	<update id="orderCancel">
		update purchasehistory set orderstatus=#{val} where orderstatus=#{condition} and DATE_ADD(startdate, INTERVAL 1 DAY) &lt; now()
	</update>
	
	<update id="resetProposalEndDateService">
		update chat_transaction set orderstatus=#{val} where orderstatus=#{condition} and DATE_ADD(endday, INTERVAL 1 DAY) &lt; now() 
	</update>
		
	<update id="orderProposalCancel">
		update chat_transaction set orderstatus=#{val} where orderstatus=#{condition} and DATE_ADD(startday, INTERVAL 1 DAY) &lt; now()
	</update>
	
</mapper>